import 'package:flutter/material.dart';

class AppColors {
  // Primary brand palette — Authority & Enterprise
  static const primary = Color(0xFF1A2B5F); // Deep Navy
  static const primaryDark = Color(0xFF0F1A3A);
  static const primaryLight = Color(0xFF2E4282);

  // Accent & Action
  static const accent = Color(0xFF0EA5E9); // Sky blue
  static const accentHover = Color(0xFF0284C7);

  // Functional / Status
  static const success = Color(0xFF10B981); // Emerald
  static const successBg = Color(0xFFECFDF5);
  static const warning = Color(0xFFF59E0B); // Amber
  static const warningBg = Color(0xFFFFFBEB);
  static const danger = Color(0xFFEF4444); // Crimson
  static const dangerBg = Color(0xFFFEF2F2);
  static const info = Color(0xFF3B82F6);
  static const infoBg = Color(0xFFEFF6FF);

  // Surface & Layout
  static const surface = Color(0xFFF8FAFC); // Clean dashboard background
  static const cardBg = Color(0xFFFFFFFF);
  static const sidebarBg = Color(0xFF0F172A); // Dark slate sidebar for desktop
  static const sidebarActive = Color(0xFF1E293B);

  // Typography & Borders
  static const textPrimary = Color(0xFF0F172A); // High contrast slate
  static const textSecondary = Color(0xFF475569);
  static const textMuted = Color(0xFF94A3B8);
  static const border = Color(0xFFE2E8F0);
  static const borderLight = Color(0xFFF1F5F9);
}
